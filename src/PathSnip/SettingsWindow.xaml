<Window x:Class="PathSnip.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PathSnip 设置"
        Width="500" Height="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Background="#F5F5F5">
    <Window.Resources>
        <!-- 基础样式 -->
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
        
        <!-- 输入框样式 -->
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#2563EB"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3B82F6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#6B7280"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#9CA3AF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#4B5563"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 下拉框样式 -->
        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <!-- CheckBox样式 -->
        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <!-- GroupBox样式 -->
        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Background="#FAFAFA" CornerRadius="4,4,0,0" Padding="12,8">
                                <TextBlock Text="{TemplateBinding Header}" 
                                           FontWeight="SemiBold" 
                                           FontSize="14"
                                           Foreground="#333333"/>
                            </Border>
                            <Border Grid.Row="1" 
                                    Background="White" 
                                    BorderBrush="#E0E0E0" 
                                    BorderThickness="0,1,1,1" 
                                    CornerRadius="0,0,4,4">
                                <ContentPresenter Margin="15,12"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- 快捷键设置 -->
                <GroupBox Header="快捷键设置" Margin="0,0,0,15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="截图快捷键：" Width="85" VerticalAlignment="Center"/>
                            <TextBox x:Name="HotkeyTextBox" 
                                     Width="200" 
                                     PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                                     GotFocus="HotkeyTextBox_GotFocus"/>
                        </StackPanel>
                        <TextBlock x:Name="HotkeyHint" 
                                   Text="点击后按下快捷键" 
                                   Foreground="#9CA3AF" 
                                   FontSize="11" 
                                   Margin="100,6,0,0"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- 存储设置 -->
                <GroupBox Header="存储设置" Margin="0,0,0,15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="保存目录：" Width="85" VerticalAlignment="Center"/>
                            <TextBox x:Name="SaveDirectoryTextBox" Width="200"/>
                            <Button Content="浏览" 
                                    Style="{StaticResource SecondaryButton}"
                                    Margin="10,0,0,0" 
                                    Padding="12,6"
                                    Click="BrowseButton_Click"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <TextBlock Text="文件名格式：" Width="85" VerticalAlignment="Center"/>
                            <TextBox x:Name="FileNameTemplateTextBox" Width="200"/>
                        </StackPanel>
                        <TextBlock x:Name="FileNameTemplateHint" 
                                   Text="支持 {yyyy} {MM} {dd} {HH} {mm} {ss} {GUID}" 
                                   Foreground="#9CA3AF" 
                                   FontSize="10" 
                                   Margin="100,4,0,0"/>
                    </StackPanel>
                </GroupBox>
                
                <!-- 剪贴板设置 -->
                <GroupBox Header="剪贴板设置" Margin="0,0,0,15">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="剪贴板模式：" Width="85" VerticalAlignment="Center"/>
                            <ComboBox x:Name="ClipboardModeComboBox" Width="200" SelectedIndex="0">
                                <ComboBoxItem Content="仅路径" Tag="PathOnly"/>
                                <ComboBoxItem Content="仅图片" Tag="ImageOnly"/>
                                <ComboBoxItem Content="图片+路径" Tag="ImageAndPath"/>
                            </ComboBox>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                            <TextBlock Text="路径格式：" Width="85" VerticalAlignment="Center"/>
                            <ComboBox x:Name="PathFormatComboBox" Width="200" SelectedIndex="0">
                                <ComboBoxItem Content="纯文本 (D:\path\image.png)" Tag="Text"/>
                                <ComboBoxItem Content="Markdown (![image](...))" Tag="Markdown"/>
                                <ComboBoxItem Content="HTML (&lt;img src=&quot;...&quot;/&gt;)" Tag="HTML"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>
                
                <!-- 系统设置 -->
                <GroupBox Header="系统设置" Margin="0,0,0,15">
                    <StackPanel>
                        <CheckBox x:Name="StartWithWindowsCheckBox" 
                                  Content="开机自动启动"
                                  Checked="StartWithWindows_Changed"
                                  Unchecked="StartWithWindows_Changed"/>
                        
                        <CheckBox x:Name="ShowNotificationCheckBox" 
                                  Content="截图后显示通知"
                                  Checked="ShowNotification_Changed"
                                  Unchecked="ShowNotification_Changed"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="ResetButton" 
                    Content="重置默认" 
                    Style="{StaticResource SecondaryButton}"
                    Margin="0,0,12,0"
                    Click="ResetButton_Click"/>
            <Button x:Name="SaveButton" 
                    Content="保存设置" 
                    Style="{StaticResource PrimaryButton}"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
